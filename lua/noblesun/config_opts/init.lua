-- When neovim calls for the directory 'config_opts', it needs a init.lua to know exactly what files to initialize with
require("noblesun.config_opts.options")
require("noblesun.config_opts.keymaps")
